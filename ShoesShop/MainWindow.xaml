<Window x:Class="ShoesShop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Shoes Shop" 
        Height="600" 
        Width="900"
        WindowStartupLocation="CenterScreen"
        Background="White"
        FontFamily="Times New Roman"
        Icon="Images/Icon.ico">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <!-- Навигация -->
            <RowDefinition Height="*"/>
            <!-- Frame -->
        </Grid.RowDefinitions>

        <!-- Header с информацией о пользователе -->
        <Border Grid.Row="0" 
                Background="#FF7FFF00" 
                Padding="10"
                BorderBrush="LightGray"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" 
                        x:Name="BackButton"
                        Content="← Назад" 
                        Padding="10,5"
                        Margin="0,0,10,0"
                        Click="BackButton_Click"
                        Visibility="Collapsed"/>

                <StackPanel Grid.Column="2" 
                           Orientation="Horizontal" 
                           x:Name="UserInfoPanel"
                           Visibility="Collapsed">
                    <TextBlock Text="Роль:" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="RoleTextBlock" FontWeight="Bold" Margin="0,0,15,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Пользователь:" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="UsernameTextBlock" FontWeight="Bold" Margin="0,0,15,0" VerticalAlignment="Center"/>
                    <Button x:Name="LogoutButton" Content="Выйти" Padding="10,5" Click="LogoutButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Навигационная панель -->
        <Border Grid.Row="1" Background="#FFEFEFEF" BorderBrush="LightGray" BorderThickness="0,0,0,1" Margin="0,0,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="Products" Margin="5" Padding="10,5" Click="ProductsButton_Click"/>
                <Button Content="Edit Product" Margin="5" Padding="10,5" Click="EditProductButton_Click"/>
                <Button Content="Orders" Margin="5" Padding="10,5" Click="OrdersButton_Click"/>
                <Button Content="Edit Order" Margin="5" Padding="10,5" Click="EditOrderButton_Click"/>
                <Button Content="Select Products" Margin="5" Padding="10,5" Click="SelectProductsButton_Click"/>
            </StackPanel>
        </Border>

        <!-- Основной Frame -->
        <Frame Grid.Row="2" x:Name="MainFrame" NavigationUIVisibility="Hidden" Navigated="MainFrame_Navigated"/>
    </Grid>
</Window>
